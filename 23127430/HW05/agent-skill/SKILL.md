---
name: performance-testing-workflow
description: Route evidence-based API performance-testing work to the repository's complete reusable implementation. Use for HW05-style Load, Stress, Spike, or Endurance design, execution, raw-log analysis, Human Review, AI critique, continuous-performance reporting, audit, or submission checks.
---

# Performance Testing Workflow

Use this file as the compatibility entrypoint retained at the repository's original `agent-skill/` path.

Before acting, read [`run-api-performance-tests/SKILL.md`](run-api-performance-tests/SKILL.md) completely and follow it as the authoritative workflow. Resolve all linked `references/` and `scripts/` relative to `run-api-performance-tests/`.

Invoke the canonical implementation as `$run-api-performance-tests` when skill discovery exposes the nested package directly.

Do not maintain a second or shortened workflow here. In particular, preserve the canonical implementation's authorization checks, explicit Human Review gate, anti-fabrication rules, genuine evidence requirements, audit integration, and exact-versus-derived metric distinctions.
